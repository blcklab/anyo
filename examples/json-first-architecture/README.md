# JSON-First Architecture Example

This example demonstrates world document 0.7 cameras, named channels, rendering intent, extension requirements, assets, prefabs, safe actions, and stable instance identity.

The referenced GLB is intentionally not bundled. Replace it with an asset you are licensed to distribute, or validate/compile the world headlessly to test the document architecture.
