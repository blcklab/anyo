import test from 'node:test'
import assert from 'node:assert/strict'
import { normalizeWorldDocument, validateWorldDocument } from '../dist/esm/index.js'
const base={version:'0.6',entities:[{id:'smoke',type:'group',position:[1,2,3],components:[{type:'anyo.vfx',effect:'sprite-particles',preset:'smoke',texture:'./fx/smoke.png',maxParticles:64,emission:{rate:12,burst:0},lifetime:{min:1,max:2},speed:{min:.1,max:.5},size:{start:.1,end:.4},spawnShape:{type:'sphere',radius:.2},space:'world'}]}]}
test('validates and normalizes renderer-neutral VFX component data',()=>{validateWorldDocument(base);const normalized=normalizeWorldDocument(base,{sourceContext:{baseUrl:'https://example.test/worlds/lab.json'}});assert.equal(normalized.entities[0].components[0].texture,'https://example.test/worlds/fx/smoke.png')})
test('rejects unsupported VFX effects and unsafe particle budgets',()=>{assert.throws(()=>validateWorldDocument({...base,entities:[{...base.entities[0],components:[{...base.entities[0].components[0],effect:'beam'}]}]}),/VFX|sprite-particles/i);assert.throws(()=>validateWorldDocument({...base,entities:[{...base.entities[0],components:[{...base.entities[0].components[0],maxParticles:0}]}]}),/maxParticles|VFX/i)})
